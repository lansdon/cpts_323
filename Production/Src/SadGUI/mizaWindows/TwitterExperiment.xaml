<UserControl x:Class="SadGUI.mizaWindows.TwitterExperiment"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:SadGUI"
             xmlns:MyWindows="clr-namespace:SadGUI.mizaWindows"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             DataContext="{Binding Path=TWITTEREXPERIMENTS}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100*"/>
        </Grid.ColumnDefinitions>

        <Button
                Grid.Column="0"
                Grid.Row="0"
                Margin="100"
                Command="{Binding Path=SpamCommand}">Spam</Button>
    </Grid>
</UserControl>
